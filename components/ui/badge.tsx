import { cn } from '@/lib/utils'
export function Badge({children,className}:{children?:React.ReactNode;className?:string}){return <span className={cn('inline-flex items-center rounded-full px-2.5 py-1 text-[11px] font-semibold',className)}>{children}</span>}
export function StatusBadge({status}:{status:string}){const s=status.toLowerCase(); const color=s.includes('closed')||s.includes('available')||s.includes('received')||s.includes('approved')||s.includes('completed')?'bg-emerald-50 text-emerald-700 dark:bg-emerald-950/40 dark:text-emerald-300':s.includes('due soon')?'bg-yellow-100 text-yellow-800 dark:bg-yellow-950/60 dark:text-yellow-200':s.includes('progress')||s.includes('assigned')||s.includes('pending')||s.includes('verification')?'bg-amber-50 text-amber-700 dark:bg-amber-950/40 dark:text-amber-300':s.includes('out')||s.includes('rejected')||s.includes('overdue')?'bg-rose-50 text-rose-700 dark:bg-rose-950/40 dark:text-rose-300':'bg-slate-100 text-slate-600 dark:bg-slate-800 dark:text-slate-300'; return <Badge className={color}>{status}</Badge>}
